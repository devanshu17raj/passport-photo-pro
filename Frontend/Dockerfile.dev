FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci --silent

COPY . .

EXPOSE 5173

# Vite must listen on 0.0.0.0 to be reachable outside the container
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
